#PF-Assgn-37

#Global variables
child_id=(10,20,30,40,50)
chocolates_received=[12,5,3,4,6]

def calculate_total_chocolates():
    tot=0
    for choc in chocolates_received:
        tot+=choc
    return tot    
    #Remove pass and write your logic here to find and 
    #return the total number of chocolates

def reward_child(child_id_rewarded,extra_chocolates):
        #chocolates_received=0
   # for cid in child_id:
        if child_id_rewarded not in child_id:
            print("Child id is invalid")
        elif (extra_chocolates<1):
            print("Extra chocolates is less than 1")
        else:
            for i in range(0,len(child_id)):
                if child_id[i]==child_id_rewarded:
                     chocolates_received[i]+= extra_chocolates
            print(chocolates_received)            
        #return chocolates_received
        # TODO: write code...
    #Remove pass and write your logic here


    # Use the below given print statements to display the output
    # Also, do not modify them for verification to work

    #print("Extra chocolates is less than 1")
    #print("Child id is invalid")
    #print(chocolates_received)


print(calculate_total_chocolates())
#Test your code by passing different values for child_id_r
#ewarded,extra_chocolates
reward_child(11,2)
